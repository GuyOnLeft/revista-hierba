{
  "name": "revista-hierba",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "playwright test"
  },
  "dependencies": {
    "astro": "^4.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
